// OPERADOR TERNÁRIO
// É uma forma curta de escrever if/else.

let idade = 16;

console.log(idade >= 18 ? "Pode dirigir" : "Não pode dirigir");

// Estrutura:
// condição ? valorSeVerdadeiro : valorSeFalso
